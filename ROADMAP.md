# Roadmap

## В работе

Ничего открытого по итогам сессии 2026-05-18. См. JOURNAL.

## Дальше

- **Миграция `/lab/frm-var` на IBM Plex Sans и общесистемные токены.**
  Сейчас лаб остался на Inter и `--v3-*` токенах из своего CSS. По
  README дизайн-системы лаб про монетку — канон, под него подгоняется
  всё остальное; этот transient state нужно закрыть отдельной сессией.
- **`/cv`** — отдельная страница. В footer контентной row сейчас её нет
  (убрана в этой сессии). Добавится ссылка, когда страница появится.
- **`/about`** — компактная About страница (часть «О себе» с текущего
  сайта переезжает сюда, не на главную).
- **`/lab`** — индекс лабов. Сейчас в footer LabCard'а есть мини-ссылка
  «архив /lab →», она ведёт в 404.
- **Решение по `.lab-door[data-portal=""]` правилам в `global.css`.**
  После переписывания главной они стали неиспользуемыми. Оставлены до
  следующей сессии (вдруг пригодятся на `/cv` или `/about`).
- **Vitest и юнит-тесты математических ядер лабов.** Когда добавится
  тест-раннер — прикрутить тесты к `scripts/lab/frm-vol-drag/`,
  `scripts/lab/frm-var-v2/` и snapshot-тест на детерминированный SVG в
  `src/components/LabCard.astro`.

## В идеях

- **Правильный favicon `D` засечкой teal.** Текущий `public/favicon.svg`
  — стилизованная гора (legacy Astro-логотип), он же лежит в
  `skill/assets/favicon.svg`. README дизайн-системы и SKILL.md
  описывают favicon как «одна буква D засечкой, изумрудная, 32×32». То
  есть и в репо, и в bundle'е favicon не соответствует собственной
  спецификации. Нужен новый SVG. См. handoff 2026-05-18 open question 4.
- **GitHub в footer.** Профиль `github.com/djoltai` существует, но пока
  пустой. Когда наполнится — добавить ссылку в footer рядом с LinkedIn.
- **OG-картинка под новую систему.** Текущая `public/og.png` сделана
  под старую главную. После того как новая главная пожила и стабильна
  — пересобрать OG в эстетике cream + IBM Plex Sans + D-засечка teal.
- **Pre-build / launch пост.** Ждёт момента, когда главная задеплоится
  на darmen.best (через Cowork).
