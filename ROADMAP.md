# Roadmap

## В работе

Ничего открытого по итогам сессии 2026-05-20. См. JOURNAL.

## Дальше

- **`/about`** — компактная About-страница. Часть «О себе» из `/cv` может
  переехать сюда, либо `/about` берёт более тёплую/личную линию, а `/cv`
  остаётся формальным. Решить при старте задачи.
- **`/lab`** — индекс лабов. Сейчас в footer LabCard'а есть мини-ссылка
  «архив /lab →», она ведёт в 404.
- **Coral в `/lab/frm-var`.** Сейчас `--v3-coral: #d97062` — литерал с
  пометкой one-off. Решить: алиасить на системный `--coral` (`#C75B4F` —
  тогда лаб станет чуть холоднее-красным), сделать `#d97062` каноном
  дизайн-системы, или закрепить one-off навсегда. Не блокер.
- **Решение по `.lab-door[data-portal=""]` правилам в `global.css`.**
  После сессии 2 (на `/cv` обычные ссылки teal, не `lab-door`) эти
  правила окончательно неиспользуемы. Можно удалить 7 строк для чистоты
  при следующем касании `global.css`.
- **Vitest и юнит-тесты математических ядер лабов.** Когда добавится
  тест-раннер — прикрутить тесты к `scripts/lab/frm-vol-drag/`,
  `scripts/lab/frm-var-v2/` и snapshot-тест на детерминированный SVG в
  `src/components/LabCard.astro`.
- **Favicon.ico пересборка.** SVG-фавикон обновлён на `D` засечкой teal
  в сессии 2; `.ico` оставлен старым (Astro mountain) — пересобрать
  растром из глифа `D` когда удобно по тулингу.

## В идеях

- **GitHub в footer.** Профиль `github.com/djoltai` существует, но пока
  пустой. Когда наполнится — добавить ссылку в footer рядом с LinkedIn.
- **OG-картинка под новую систему.** Текущая `public/og.png` сделана
  под старую главную. После того как новая главная пожила и стабильна
  — пересобрать OG в эстетике cream + IBM Plex Sans + D-засечка teal.
  Также — отдельная OG-картинка под `/cv`.
- **Pre-build / launch пост.** Ждёт момента, когда обновлённый сайт
  (главная + `/cv`) задеплоится на darmen.best (через Cowork).
- **Favicon `<path>` вместо `<text>`.** Сейчас favicon.svg использует
  `<text>` с EB Garamond fallback-стеком — достаточно надёжно. Для
  пиксель-идентичного рендера без зависимости от шрифта — извлечь глиф
  `D` из EB Garamond 600 и пересохранить как `<path>`.
