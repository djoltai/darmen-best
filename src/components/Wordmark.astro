---
---

<span class="wordmark">
  <span class="wordmark-d">D</span><span>armen.best</span>
</span>

<style>
  .wordmark {
    font-family: var(--font);
    font-weight: var(--w-medium);
    font-size: 18px;
    letter-spacing: -0.025em;
    display: inline-flex;
    align-items: baseline;
    color: var(--ink);
  }
  .wordmark-d {
    font-family: var(--font-serif);
    font-weight: var(--w-strong);
    color: var(--teal);
    letter-spacing: -0.02em;
  }
</style>
