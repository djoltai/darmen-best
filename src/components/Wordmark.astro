---
---

<a href="/" class="wordmark" aria-label="darmen.best — на главную">
  <span class="wordmark-d">D</span><span>armen.best</span>
</a>

<style>
  a.wordmark {
    font-family: var(--font);
    font-weight: var(--w-medium);
    font-size: 18px;
    letter-spacing: -0.025em;
    display: inline-flex;
    align-items: baseline;
    color: var(--ink);
    border-bottom: none;
    text-decoration: none;
  }
  a.wordmark:hover {
    color: var(--ink);
    border-bottom: none;
  }
  .wordmark-d {
    font-family: var(--font-serif);
    font-weight: var(--w-strong);
    color: var(--teal);
    letter-spacing: -0.02em;
  }
</style>
